<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sanad.mobile">

    <!-- Baseline Network & System Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Audio Input for Palestinian Dialect Voice Assistant -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Address Book: Contact Read (sanad:perm:contacts:read) -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- GATED / DEFERRED CAPABILITIES PERMISSION DECLARATIONS -->
    <!-- The following permissions are declared for future Phase 3 authorization, -->
    <!-- but are currently strictly gated and inactive behind the Security Kernel. -->

    <!-- Telephony: Bounded Phone Calling (CAP_CONTACT_CALL - GATED) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Messaging: SMS Dispatch (CAP_MESSAGE_SEND - GATED) -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Geolocation: Fine & Coarse Location (CAP_LOCATION_READ / CAP_LOCATION_SHARE - GATED) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Calendar Integration: Read & Write (CAP_CALENDAR_READ / CAP_CALENDAR_WRITE - GATED) -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
